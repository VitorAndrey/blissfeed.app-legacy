import { Text } from "@ui//Text";
import { SafeAreaView } from "react-native-safe-area-context";

export function CompleteSignUp() {
  return (
    <SafeAreaView className="flex-1">
      <Text>CompleteSignUp</Text>
    </SafeAreaView>
  );
}
